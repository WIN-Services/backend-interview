version: '3.8'
services:
  mongo:
      image: mongo
      restart: always
      ports:
         - "27017:27017"
  server:
    build: .
    ports: 
      - "3000:3000"
    depends_on:
         - "mongo"
    environment:
         - CONNECTIONSTRING=mongodb://mongo:27017/userManagement


      